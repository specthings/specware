SPDX-License-Identifier: CC-BY-SA-4.0 OR BSD-2-Clause
copyrights:
- Copyright (C) 2023, 2026 embedded brains GmbH & Co. KG
enabled-by: true
links:
- role: spec-member
  uid: root
- role: spec-refinement
  spec-key: non-functional-type
  spec-value: design-target
  uid: requirement-non-functional
spec-description: null
spec-example: null
spec-info:
  dict:
    attributes:
      brief:
        description: |
          If the value is present, then it shall briefly describe the target.
        spec-type: optional-str
      description:
        description: |
          If the value is present, then it shall thoroughly describe the target.
        spec-type: optional-str
      name:
        description: |
          It shall be the target name.
        spec-type: str
      key:
        description: |
          It shall be the target key.
        spec-type: str
      target-hash:
        description: |
          It shall be the target hash.
        spec-type: str
    description: |
      This set of attributes specifies a design ${/glossary/target:/term}.
      Targets shall provide

      - runtime performance limits through
        ${performance-runtime-limits-provider-role:/spec-name} links to
        ${performance-runtime-limits-provider:/spec-name} items,

      - test timeouts through ${test-timeouts-role:/spec-name} links to
        ${test-timeouts:/spec-name} items, and

      - test failure and code coverage gap verifications through
        ${verification-provider-role:/spec-name} links to
        ${requirement-design-target:/spec-name}, ${verification:/spec-name}, or
        ${verification-group:/spec-name} items.
    mandatory-attributes: all
spec-name: Design Target Item Type
spec-type: requirement-design-target
type: spec
